@extends('layouts.admin', ['title' => 'Choose event type', 'heading' => 'What type of event is this?', 'subheading' => 'Competition and concert bookings collect different information.'])
@section('content')@include('admin.event-management._tabs')<div class="grid two-col">@foreach($eventTypes as $eventType)<a class="card card-pad" href="{{ $eventType->system_category->value === 'competition' ? route('admin.scheduling-events.create', ['type' => 'competition', 'event_type' => $eventType->uuid]) : route('concert-bookings.create', ['event_type' => $eventType->uuid]) }}"><h2>{{ $eventType->name }}</h2><p class="muted">{{ $eventType->description ?: 'Uses the '.str($eventType->system_category->value)->title().' workflow.' }}</p></a>@endforeach</div>@endsection
