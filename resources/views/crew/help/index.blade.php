@extends('layouts.crew', ['title' => 'My Handbook'])
@section('content')
<div class="page-heading"><div><div class="crew-hub-brand"><img src="{{ asset('images/brand/dancepro-icon-blue.png') }}" alt=""><span>CREW HUB</span></div><h1>My Handbook</h1><p class="muted">Processes, role guides, safety information and event-day support.</p></div></div>
<section class="card profile-section"><h2>Help & Support</h2><p>If something is unclear or goes wrong, check the relevant guide below and message DancePro with the event name, your role and exactly what happened. For an immediate safety emergency, move away from danger and call 000.</p><p class="muted">Direct in-app messaging and incident reporting will be added in the communications stage.</p></section>
<section><div class="resource-grid">@forelse($resources as $resource)<article class="card resource-card"><div><span class="type-label">{{ $resource->section_number ? 'Section '.$resource->section_number : str_replace('_',' ',$resource->resource_type) }}</span><h2>{{ $resource->title }}</h2><p class="muted">{{ $resource->summary }}</p></div>@if($resource->content)<div class="resource-copy">{!! nl2br(e($resource->content)) !!}</div>@endif<div class="resource-actions">@if($resource->fileUrl())<a class="button" href="{{ $resource->fileUrl() }}" target="_blank" rel="noopener">Open {{ str_ends_with(strtolower($resource->file_path),'.pdf') ? 'PDF' : 'guide' }} ↗</a>@endif @if($resource->external_url)<a class="button secondary" href="{{ $resource->external_url }}" target="_blank" rel="noopener">Open link ↗</a>@endif</div></article>@empty<div class="card empty-state"><strong>Resources are being prepared.</strong><p class="muted">Help and handbook files will appear here.</p></div>@endforelse</div></section>
@endsection
